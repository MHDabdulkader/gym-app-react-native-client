import { Text, View } from "react-native";
import { SafeAreaProvider } from "react-native-safe-area-context";

export default function Weight_Screen_screen(){
    return(
        <SafeAreaProvider>
            <View>
                <Text>
                    Set Gender
                </Text>
            </View>
        </SafeAreaProvider>
    )
}